# note記事内テーブル画像化ルール

note記事内のMarkdownテーブルは、以下のルールでPNG画像化する。

## 基本方針

* Markdownテーブルの文字列を画像生成AIで描画しない。
* HTML/CSS、SVG、Pillow等の決定論的な方法でレンダーする。
* 表内の文言は元Markdownからそのまま取得し、内容を変更・要約・言い換えしない。
* 既存の `note/images/table*_{ja,en}.png` がある場合、それらをデザイン上の基準とする。
* 同一記事内では、すべてのテーブル画像のデザインを統一する。

## 画像サイズ

* **横幅は原則固定する。**
* **高さは固定しない。**
* 高さは、ヘッダー・行数・セル内の折り返し量に応じて自動決定する。
* 行数が少ない表を無理に縦長に引き伸ばさない。
* 行数が多い場合も、文字や行間を過度に縮小して固定サイズへ押し込まない。
* 上下左右には一定の余白を設ける。

つまり、

**width = fixed
height = auto**

を原則とする。

## レイアウト

* ヘッダーは既存画像と同じ背景色・文字スタイルを使用する。
* 本文は白地を基本とする。
* 列境界・行境界には既存画像と同等の罫線を使用する。
* セル内には適切なpaddingを設ける。
* 長い文章はセル幅に応じて自然に折り返す。
* 行の高さは、その行で最も高さを必要とするセルに合わせる。
* 表全体を画像中央に配置するためだけの大きな空白は作らない。

## 文字

* 日本語・英語ともに読みやすいSans Serif系フォントを使用する。
* 日本語文字を完全に表示できるフォントを優先する。
* フォントサイズは原則として全テーブルで統一する。
* 表が大きいという理由だけで安易にフォントを縮小しない。
* 文字列の正確性を最優先する。

## アイコン・装飾

* 既存画像に行アイコンや色分けなどの視覚表現がある場合、その規則を踏襲する。
* 装飾は表の意味構造を補助する目的に限定する。
* 元Markdownにない意味を勝手に追加しない。
* 既存画像から規則を推定できる場合は、人間に問い合わせる前に既存画像を参照する。

## ファイル名

日本語版：

`note/images/tableN_ja.png`

英語版：

`note/images/tableN_en.png`

`N` は記事内での出現順とする。

既存画像がある場合は、既存番号との衝突を避け、連番を継続する。

## Markdownの置換

画像化したMarkdownテーブルは、元の位置で次の形式に置換する。

`TODO: tableN_ja.png`

英語版の場合：

`TODO: tableN_en.png`

テーブル前後の本文・見出し・空行など、テーブル以外の内容は変更しない。

## 検証

生成後は必ず以下を確認する。

1. 元Markdownの全テーブルが検出されていること。
2. 不可視文字等を含むテーブルも取りこぼしていないこと。
3. 表の全文言が元Markdownと一致していること。
4. 列数・行数が一致していること。
5. 文字切れ・はみ出し・不自然な折り返しがないこと。
6. 行数の少ない表に不要な縦方向の空白がないこと。
7. 既存のテーブル画像とデザイン上の一貫性があること。
8. 対応する `TODO: tableN_{ja,en}.png` が正しい位置に挿入されていること。
9. 成果物に他ファイルへの参照が含まれる場合、完了報告前に全参照先の実在を機械的に検証する。参照数とファイル数の一致だけではなく、参照集合と実在集合の差集合が空であることを確認する。

## 設計原則

**画像サイズに表を合わせるのではなく、表に画像サイズを合わせる。**

横幅によって記事内での見た目を統一し、縦方向はコンテンツ量に従わせる。

# note記事内の引用（blockquote）貼り付けルール

## 症状

Markdownの `>` による引用（複数行、途中に空行や番号リストを含むもの）をレンダリングしてnoteのウェブエディターに貼り付けると、空行以降の内容だけ文字が小さくなり右詰めになる。まるで引用本文と「出典」に分割されたような見た目になる。

空行に無幅スペース・半角スペース・全角スペースなど、どの文字を入れても症状は変わらない。

## 原因（noteの内部仕様は非公開のため推測）

原因は空行に入れる**文字の中身**ではなく、貼り付け元HTMLの**構造**にある。

Markdownの引用内に空行を挟むと、レンダリング後は同一の `<blockquote>` 内に**複数の段落（`<p>`）**が生成される。noteの貼り付け処理は、この「引用内に複数段落がある」という構造を「引用本文＋出典」の2部構成とみなし、後半を小さく・右寄せのスタイルに変換していると考えられる。

## 対処

1. レンダリング済みHTMLをそのまま貼り付けない。
2. noteのエディター上で、引用にしたい範囲をプレーンテキストとして入力または貼り付ける。
3. 改行はすべて **Shift+Enter（段落を割らない改行）** で入れる。Enterキーによる段落区切りを作らない。
4. 全体を選択し、noteのツールバーの「引用」ボタンでブロッククオート化する。
5. 番号リストを含む場合、noteの自動リスト変換（`1. ` 入力での自動箇条書き化）が働くとブロックが引用の外に飛び出すことがあるため、自動変換を避けるか、変換後に手動で引用ブロック内へ戻す。

## 検証状況

**未検証。** 2026-08-11時点でこの手順はまだユーザーによる実機確認が取れていない。この手順を適用した場合は、実際にnoteのプレビューで小さく右詰めになる症状が再現しないことを目視で確認すること。
